on: [push]

jobs:
  upload_files:
    runs-on: ubuntu-latest
    name: Upload a builded file.
    steps:
    - name: Checkout
      uses: actions/checkout@v2.3.4
    - name: Upload Files
      id: upload
      uses: Creepios/sftp-action@v1.0.1
      with:
        host: 'ip-69-164-194-46.cloudezapp.io'
        port: 22122
        username: 'sip3'
        password: '@Fsp2011br'
        localPath: './sip'
        remotePath: './'
